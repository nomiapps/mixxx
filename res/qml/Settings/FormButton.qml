import ".." as Skin

// One button for the whole chrome. This file used to be a byte-for-byte copy of
// res/qml/FormButton.qml with its own colours; the settings pages import it by
// this path, so the path stays and the implementation is shared.
Skin.FormButton {
}
